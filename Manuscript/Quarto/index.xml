<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE article PUBLIC "-//NLM//DTD JATS (Z39.96) Journal Archiving
and Interchange DTD v1.2 20190208//EN" "JATS-archivearticle1.dtd">

<article xmlns:mml="http://www.w3.org/1998/Math/MathML" xmlns:xlink="http://www.w3.org/1999/xlink" dtd-version="1.2" article-type="other">

<front>


<article-meta>


<title-group>
<article-title>The Art of Scientific computation</article-title>
<subtitle>A comparison of techniques for simulation of the abelian
sandpile model</subtitle>
</title-group>

<contrib-group>
<contrib contrib-type="author" corresp="yes">
<name>
<surname>Reuben Bender</surname>
<given-names></given-names>
</name>
<string-name>(James) Reuben Bender</string-name>

<role vocab="https://credit.niso.org" vocab-term="writing – original
draft" vocab-term-identifier="https://credit.niso.org/contributor-roles/writing-original-draft/">writing</role>
<xref ref-type="aff" rid="aff-1">a</xref>
<xref ref-type="corresp" rid="cor-1">&#x002A;</xref>
</contrib>
</contrib-group>
<aff id="aff-1">
<institution-wrap>
<institution>The University of Melbourne</institution>
</institution-wrap>







</aff>
<author-notes>
<corresp id="cor-1"></corresp>
</author-notes>









<history></history>






</article-meta>

</front>

<body>
<sec id="the-abelian-sandpile-model">
  <title>The abelian sandpile model</title>
  <p>The abelian sandpile model is a cellular automaton that models the
  behavior of a pile of sand grains. The model is defined on a lattice,
  where each point (site) can hold a certain number of grains (i.e. be
  at a certain ‘height’). When a site reaches a certain height, it
  becomes unstable and topples, sending one grain to each of its
  neighbors. This spread of sand can cause a chain reaction of topplings
  -an avalanche- that can propagate through the lattice. While each
  topple is simple and deterministic, predicting the size and shape of
  the avalanches on a large lattice can be non-trivial - a problem
  compounded as we model the behaviour of the system over time (i.e. as
  we continue to add grains). Therefore, the abelian sandpile model is a
  good candidate for computational simulation. By repeating the
  toppling/avalance process iteratively, we can might hope to
  characterise statistical features of the system, such as the
  distribution of avalanche sizes (both in terms of number of topples
  and spatial scale), the average number of grains present on the
  lattice, or the</p>
  <p>When implimenting a sumultation of the ASM, we do not have the
  option of employing an infinite sized grid. Instead, we must choose a
  finite grid size, and implement special transition conditions at the
  boundaries.</p>
  <p>When using a finite grid and losing grains at the edges, our output
  is subject to so called ‘finite size effects’, whereby extreme events
  are less common than we would expect on an infinite grid. This is
  because the avalanches are more likely to be cut off by the
  boundaries. To a ### Applications of the BTW model e.g. simulation of
  physical systems, traffic flow, forest fires, epidemiological models
  etc. ## Scientific programming tools</p>
  <p>In large sandpiles, which have the potential for large scale
  avalanves (e.g. involving millions of topples), the computational
  tools and techniques used to simulate the model are crucial. A
  secomdary aim of this progject (in adittion to presenting the BTW
  model) is to explore several alternative computational techniques for
  developing and simulating the model. There are two main aspects I will
  consider:</p>
  <p>Algorithm design: The way in which the toppling and stabilisation
  algorithms implemented can have a significant impact on the efficiency
  of the simulation. These design decisions will depend on the backend
  on which the simulation is run - e.g. Does the algorithm complexity
  scale efficiently with the lattice size (to avoid memory bottlenecks)?
  Can the algorithm make use of the multithreading capabilities of
  modern CPUs (or, moreover, GPUs)? Therefore, there is unlikely to be a
  single optimal algorithm, but rather a trade-off between different
  factors. I will compare the performance of several different
  algorithms for simulating the BTW model.</p>
  <p>Programming languages: The choice of programming language can also
  have a significant impact on the efficiency of simulation; both in the
  sense of the time taken to run the simulation, and the ease of
  developing and maintaining code. Modern scientific programming
  typically falls into a two-language paridigm, wherein a relatively
  high-level, dynamic language (usually python) is used for rapid
  prototyping and data analysis, and a lower-level, statically typed and
  compiled language (e.g. C, C++, Fortran) is used for computationally
  intensive tasks. There is some middle ground here with the use of high
  level wrappers in dynamic languages that call high performance
  compiled code for performance critical tasks (e.g. numpy, scipy, numba
  in python).</p>
  <p>While these approaches well established and can result in highly
  efficient implimentations, the two language approach has drawbacks.
  The most obvious is the need to develop (and potentially maintain) two
  separate codebases. Moreover, the specialised skills needed to develop
  and maintain high performance code in a low-level language can be a
  barrier to entry for many researchers. This barrier can be compounded
  by the discontinuity in source material between two codebases. For
  example, localising and adressing bugs can require more elaborate
  error handling and logging when the code is split between two
  languages.</p>
  <p>The Julia language was developed as an alternative to the
  two-language approach @. A high level, dynamic language Julia’s
  progressive typing and type inference system, along with just-in-time
  compilation, intends to allow for easily readable and maintainable
  code that can be optimised for performance without leaving the
  language - a language that ‘walks like python, runs like C’.</p>
  <p>This report will present a comparison of the performance of several
  different algorithms for simulating the BTW model, implemented in
  Julia and python. The aim is to explore the trade-offs between
  algorithm design and programming language choice, and to potentially
  to inform students or researchers devevloping their own simulations of
  the BTW model.</p>
</sec>
<sec id="simulation-algorithms">
  <title>Simulation Algorithms</title>
  <sec id="toppling-algorithms">
    <title>Toppling Algorithms</title>
    <sec id="push-topple-algorithm">
      <title>‘Push’ topple algorithm</title>
      <p>The naive algorithm is naive in the sense that it is the most
      straightforward way to implement the toppling algorithm, but also
      in the sense that it does not assume any information about where
      topples might occur (i.e. it checks every site, c.f. the targeted
      algorithm). The algorithm iterates over each site in the lattice,
      and if the site has more sand than the threshold, it topples and
      sends sand to its neighbors.</p>
      <preformat>#| label: alg-push_topple
#| html-indent-size: &quot;1.2em&quot;
#| html-comment-delimiter: &quot;//&quot;
#| html-line-number: true
#| html-line-number-punc: &quot;:&quot;
#| html-no-end: false
#| pdf-placement: &quot;htb!&quot;
#| pdf-line-number: true

\begin{algorithm}
\caption{Push toppling algorithm}
\begin{algorithmic}
\input{a $n \times m$ lattice $X$, topple threshold $k$, a site $i$}
\output{a $n \times m$ lattice $Y$}
  \If {$X_{i} \geq k$}
      \State $Y_{i} \gets X_{i} - k$
      \For{each neighbor $j$ of $i$}
          \State $Y_j \gets X_j + 1$
      \EndFor
  \Else
      \State $Y_i \gets X_i$
  \EndIf 
\end{algorithmic}
\end{algorithm}</preformat>
      <p>Because the every topple event using the naive algorithm
      effects the output of five locations on the lattice, the algorithm
      is not suitable for parallelization: the order of the for loop
      must be preserved to prevent multiple threads from updating the
      same site simultaneously (which would potentially lead to
      conflicting results when checking if a site is above the
      threshold). To remedy this limitation, we can use the parallel, or
      ‘pull’ algorithm</p>
    </sec>
    <sec id="pull-topple-algorithm">
      <title>‘pull’ topple algorithm</title>
      <p>The parrallel is based on the idea of “pulling” sand from
      neighbors rather than “pushing” sand to neighbors. Each cell is
      updated by checking each of it’s neighbours - for each neighbour
      that is ready to topple, the cell gains one grain. If the cell
      itself is ready to topple, it loses four grans. Each iteration of
      the for loop therefore only changes one site (i.e. each iteration
      is encapsulated), and therefore each cell can be updated in
      parallel.</p>
      <preformat>#| label: alg-pull_topple
#| html-indent-size: &quot;1.2em&quot;
#| html-comment-delimiter: &quot;//&quot;
#| html-line-number: true
#| html-line-number-punc: &quot;:&quot;
#| html-no-end: false
#| pdf-placement: &quot;htb!&quot;
#| pdf-line-number: true

\begin{algorithm}
\caption{Pull toppling algorithm}

\begin{algorithmic}
\input{ a $n \times m$ lattice $X$, topple threshold $k$, a site $i$}
\output{a $n \times m$ lattice $Y$}
  \If {$X_i \geq k$}
      \State $Y_i$ $\gets$ $X_i - k$
  \Else
      \State $Y_i$ $\gets$ $X_i$
  \EndIf

  \For {each neighbor $j$ of $i$}
      \If {$X_j \geq k$}
          \State $Y_i$ $\gets$ $Y_i + 1$
      \EndIf
  \EndFor
\end{algorihmic}
\end{algorithm}</preformat>
    </sec>
  </sec>
  <sec id="stabilisation-algorithms">
    <title>Stabilisation algorithms</title>
    <sec id="naive-stabilisation-algorithm">
      <title>Naive stabilisation algorithm</title>
      <p>The naive stabilisation algorithm is naive in the sense that it
      is the most straightforward way to implement the toppling
      algorithm, but also in the sense that it does not assume any
      information about where topples might occur. It iterates over each
      site in the lattice, and if the site has more sand than the
      threshold, it topples. This loop is repeated until each cell is
      below the toppling threshold. This algorithm works effectively
      with both push and pull toppling algorithms.</p>
      <preformat>#| label: alg-naive_stabilise
#| html-indent-size: &quot;1.2em&quot;
#| html-comment-delimiter: &quot;//&quot;
#| html-line-number: true
#| html-line-number-punc: &quot;:&quot;
#| html-no-end: false
#| pdf-placement: &quot;htb!&quot;
#| pdf-line-number: true

\begin{algorithm}
\caption{Naive stabilisation algorithm}

\begin{algorithmic}
\input{ a $n \times m$ lattice $X$, topple threshold $k$}  
\output{ $n \times m$ lattice $Y$}
\While {any site in $X$ is above $k$}
  \For {each site $i$ in $X$}
      \If {$X_i \geq k$}
          \State $Y$ $\gets$ topple!(X, i)
      \Else
          \State $Y_i$ $\gets$ $X_i$
      \EndIf 
  \EndFor
\EndWhile
\end{algorihmic}
\end{algorithm}</preformat>
    </sec>
  </sec>
  <sec id="targeted-stabilisation-algorithm">
    <title>Targeted stabilisation algorithm</title>
    <p>This algorithm assumes we have information about the locations of
    potential topple sites. For example, when dropping a single grain of
    sand, we first can check if the drop site needs to toppled. If it
    doesn’t, there is no possibility of avalance and no other sites need
    to be checked. If it does, can topple that site and just check the
    the neighbors to which the sand spreads. While needing less checks,
    we now must keep track of the sites that need to be checked. It is
    expected that this algorithm will provide performance improvements
    when the number of sites that need to be checked is significantly
    less than the total number of sites (i.e. a ‘sparse’ avalance)</p>
    <preformat>#| label: alg-targeted_topple
#| html-indent-size: &quot;1.2em&quot;
#| html-comment-delimiter: &quot;//&quot;
#| html-line-number: true
#| html-line-number-punc: &quot;:&quot;
#| html-no-end: false
#| pdf-placement: &quot;htb!&quot;
#| pdf-line-number: true

\begin{algorithm}
\caption{Targeted toppling algorithm}

\begin{algorithmic}
\input{ a $n \times m$ lattice $X$, topple threshold $k$, A list of sites to check $L$}  
\output{  sandpile model lattice $Y$}
\While {$L$ is not empty}
    \State remove element $i$ from $L$
    \If {$X_i \leq k$}
        \State $Y \gets$ topple!(X, i)
    \EndIf
\EndWhile
\end{algorihmic}
\end{algorithm}</preformat>
  </sec>
</sec>
</body>

<back>
</back>

<!-- (F2ED4C6E)[nb-1]:C:\Users\Reubender\Projects\TASC\Manuscript\Quarto\Algorithms.qmd -->
<!-- (F2ED4C6E)[nb-2]:C:\Users\Reubender\Projects\TASC\Manuscript\Quarto\Introduction.qmd -->
<!-- (F2ED4C6E)[nb-3]:C:\Users\Reubender\Projects\TASC\Manuscript\Quarto\Languages.qmd -->
<!-- (F2ED4C6E)[nb-4]:C:\Users\Reubender\Projects\TASC\Manuscript\Quarto\Methods.qmd -->
<!-- (F2ED4C6E)[nb-5]:C:\Users\Reubender\Projects\TASC\Manuscript\Quarto\snippets\topple_algs.qmd -->

</article>